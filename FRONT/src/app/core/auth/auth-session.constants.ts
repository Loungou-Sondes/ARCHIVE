/** Déconnexion automatique après cette durée sans interaction utilisateur. */
export const IDLE_TIMEOUT_MS = 30 * 60 * 1000;

/** Durée maximale de session (alignée sur app.session.max-age-seconds côté serveur). */
export const SESSION_MAX_MS = 60 * 60 * 1000;
